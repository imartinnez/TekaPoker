VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...

# ── Solo para el analista IA (chat) ──────────────────────────
# NO va en este archivo en producción: se configura como variable
# de entorno en Vercel (Settings → Environment Variables), porque es
# secreta y se usa solo en el servidor (frontend/api/chat.js).
#   GROQ_API_KEY=gsk_...            (obligatoria, de console.groq.com)
#   GROQ_MODEL=llama-3.3-70b-versatile   (opcional)
